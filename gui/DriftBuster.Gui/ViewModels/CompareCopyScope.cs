namespace DriftBuster.Gui.ViewModels
{
    /// <summary>What a copy holds: the setting and every value, the setting's name, every value, or the right-clicked value.</summary>
    public enum CompareCopyScope
    {
        All,
        Setting,
        Values,
        ThisValue,
    }
}
