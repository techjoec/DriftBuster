namespace DriftBuster.Gui.ViewModels
{
    /// <summary>What history to show: where the setting is set and how it changed, where the value appears, or both.</summary>
    public enum CompareHistoryKind
    {
        Setting,
        Value,
        Both,
    }
}
