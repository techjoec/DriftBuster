# DriftBuster 0.0.3

## Core
- Add `registry-live` format plugin to detect registry scan definition files.
- Introduce Windows Registry live scan utilities (enumerate apps, suggest roots, search).
- Offline runner can execute `registry_scan` sources and package results.
- Realtime secret masking with DPAPI/AES encryption flow.
- Scheduler orchestration for recurring profile runs.

## Formats
- Registry Live: JSON and YAML manifest detection with `registry_scan` token/keywords/patterns; metadata captured for downstream tools.

## GUI
- **Multi-server tab**: Compare up to 6 servers with drag-to-reorder host cards, root validation, session persistence, and activity timeline.
- Surfaces `registry_scan.json` outputs (when present) alongside file-based findings.
- Toast telemetry for scan completions and errors.
- Profile scheduler with notification adapters.

## Installer
- None.

## Tooling
- Avoid test import collision by forcing local `scripts` package during pytest collection.
- Coverage guardrails enforced for all GUI components.

## Cumulative Features

> All features available in this release.

### GUI Tabs
- [x] Diff planner - Compare configuration snapshots with baseline selection
- [x] Hunt explorer - Secret/identifier scanning with regex rules
- [x] Profiles - Profile management, scheduling, and recurring runs
- [x] Multi-server - Multi-host orchestration (up to 6 servers) with drag-to-reorder

### Format Plugins
- [x] JSON (0.0.3), XML (0.0.6), YAML (0.0.3), TOML (0.0.3), INI (0.0.2)
- [x] HCL, Dockerfile, text (0.0.2), binary
- [x] Registry Live (new)

### CLI Tools
- [x] `python -m driftbuster.cli` - Main scanning CLI
- [x] `python -m driftbuster.multi_server` - Multi-server orchestration
- [x] `python -m driftbuster.offline_runner` - Process pre-captured snapshots
- [x] `driftbuster-export-sql` - SQLite export with masking/hashing
- [x] `python -m driftbuster.registry_cli` - Windows Registry live scan

### PowerShell Module
- [x] `Invoke-DriftBusterDiff` - Run diffs from PowerShell
- [x] `Export-DriftBusterSqlSnapshot` - SQL export with column masking

### Session Persistence
- [x] Multi-server session caching (labels, scopes, roots, baseline preferences)
- [x] Diff planner MRU persistence
- [x] Profile schedules persistence

## Notes
- Windows-only functionality for live registry scans; non-Windows hosts skip gracefully.
- See docs/registry.md for Registry API and offline-runner usage.
- See docs/windows-gui-guide.md for multi-server usage.
