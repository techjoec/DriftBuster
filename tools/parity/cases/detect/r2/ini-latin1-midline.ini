; ascii padding so looks_text accepts the sample
; ascii padding so looks_text accepts the sample
; ascii padding so looks_text accepts the sample
; ascii padding so looks_text accepts the sample
; ascii padding so looks_text accepts the sample
; ascii padding so looks_text accepts the sample
[sec]
name=café au lait
k=v
