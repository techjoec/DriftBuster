{
  "description": "the preferred baseline skips an unreadable file (fix b) and still holds the shared app.ini (self-diff pane); a later host holds the file the baseline skipped, so that entry falls back to the smallest host id",
  "fixes": ["a", "b"], "args": [], "requires": ["unprivileged"],
  "plans": [
    {"host_id": "zmixed", "label": "Mixed", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/zero/mixed"], "baseline": {"is_preferred": true, "priority": 5, "role": "auto"}},
    {"host_id": "bplain", "label": "PlainB", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r2/errmap/good"]},
    {"host_id": "aother", "label": "OtherA", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/subdirs/other"]}
  ]
}
