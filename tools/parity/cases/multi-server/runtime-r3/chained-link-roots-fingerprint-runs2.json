{
  "description": "roots reached through a chain of relative links whose target holds .. after a link (the kernel resolves the parent of the link's target; a lexical resolution lands in the c-target-lexical tree), an absolute link to that chain, the same two roots in both orders on two hosts (the fingerprint sorts resolved roots) and the lexical tree on its own; second run reuses the cache by signature",
  "fixes": [], "args": ["--runs", "2"], "requires": [],
  "plans": [
    {"host_id": "chain", "label": "Chain", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/chain/a"], "baseline": {"is_preferred": true, "priority": 1, "role": "auto"}},
    {"host_id": "abs", "label": "Abs", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/chain/abs-to-a", "$PARITY_MULTI_SERVER_WORK/r3/chain/b"]},
    {"host_id": "rev", "label": "Rev", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/chain/b/../c", "$PARITY_MULTI_SERVER_WORK/r3/chain/abs-to-a/../d"]},
    {"host_id": "lexical", "label": "Lexical", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/chain/c/d"]}
  ]
}
