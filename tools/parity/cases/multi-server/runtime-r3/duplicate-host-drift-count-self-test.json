{
  "description": "one drifting host id planned twice next to a baseline: drift_count counts the host id once while two drilldown rows drift; the harness self-test then proves the aggregate check rejects a drift_count one below the right one, one counted per plan and a wrong severity",
  "fixes": [], "args": [], "requires": [], "self_test": "wrong-drift-count",
  "plans": [
    {"host_id": "base", "label": "Base", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/plain/hostB"], "baseline": {"is_preferred": true, "priority": 1, "role": "auto"}},
    {"host_id": "dup", "label": "First", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/plain/hostC"]},
    {"host_id": "dup", "label": "Second", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/plain/hostC"]}
  ]
}
