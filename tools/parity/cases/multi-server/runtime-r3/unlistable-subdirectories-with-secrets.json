{
  "description": "a root holding a mode-000 subdirectory and a mode-0300 subdirectory (listing refused) with secrets, next to a readable secret: both walks skip the refused directories silently, so the host succeeds and has_secrets comes from the readable file only",
  "fixes": ["a"], "args": [], "requires": ["unprivileged"],
  "plans": [
    {"host_id": "host", "label": "Host", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/subdirs/host"], "baseline": {"is_preferred": true, "priority": 1, "role": "auto"}},
    {"host_id": "other", "label": "Other", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r3/subdirs/other"]}
  ]
}
