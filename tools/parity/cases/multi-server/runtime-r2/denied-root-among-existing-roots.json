{
  "description": "one plan whose roots are a good directory, a missing root and a mode-000 directory: existing roots are the good and locked ones; the locked root fails the whole host with roots = plan.roots",
  "fixes": ["b"], "args": [], "requires": ["unprivileged"],
  "plans": [
    {"host_id": "good", "label": "Good", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r2/errmap/good"], "baseline": {"is_preferred": true, "priority": 10, "role": "auto"}},
    {"host_id": "mixed", "label": "Mixed", "scope": "custom_roots", "roots": ["$PARITY_MULTI_SERVER_WORK/r2/errmap/good", "$PARITY_MULTI_SERVER_WORK/r2/errmap/none", "$PARITY_MULTI_SERVER_WORK/r2/errmap/locked"]}
  ]
}
