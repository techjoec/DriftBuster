{
  "description": "a host whose only root is a file against a directory host; fix a: the file root's relative path is '.', so its id no longer meets server02's appsettings.json",
  "fixes": ["a"],
  "args": [],
  "requires": [],
  "plans": [
    {"host_id": "single", "label": "Single file", "scope": "custom_roots", "roots": ["fixtures/multi-server/server01/app/appsettings.json"], "baseline": {"is_preferred": true, "priority": 10, "role": "auto"}},
    {"host_id": "server02", "label": "server02", "scope": "custom_roots", "roots": ["fixtures/multi-server/server02"]}
  ]
}
