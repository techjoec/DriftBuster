{
 "description": "the baseline plan's label holds a lone surrogate and heads only empty diffs: Python raises nothing and keeps the surrogate in every string, the drilldown's diff_summary labels and versions included; the port's diff_summary JsonElement holds U+FFFD there (diff-summary-replacement), every other string is compared byte for byte",
 "fixes": [],
 "args": [],
 "requires": [],
 "divergences": [
  "diff-summary-replacement"
 ],
 "plans": [
  {
   "host_id": "hostA",
   "label": "host\udfffA",
   "scope": "custom_roots",
   "roots": [
    "tools/parity/cases/multi-server/algorithm-r3/trees/surrogate/hostB"
   ],
   "baseline": {
    "is_preferred": true
   }
  },
  {
   "host_id": "hostB",
   "label": "hostB",
   "scope": "custom_roots",
   "roots": [
    "tools/parity/cases/multi-server/algorithm-r3/trees/surrogate/hostB"
   ]
  }
 ]
}
